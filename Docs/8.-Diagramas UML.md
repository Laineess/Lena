# 08. Diagramas UML

**Proyecto:** Leña — Sistema de comandas y gestión de restaurantes pequeños
**Versión del documento:** 1.0
**Fecha:** 2026-07-15
**Autor:** Carlos González Laines
**Depende de:** [02. Arquitectura](./2.-Arquitectura.md), [04. Modelo de datos](./4.-Modelo%20de%20datos.md), [05. Casos de uso](./5.-Casos%20de%20uso%20y%20diagramas%20de%20estado.md)

## Registro de cambios

| Versión | Fecha | Autor | Cambio |
|---|---|---|---|
| 1.0 | 2026-07-15 | Carlos González Laines | Diagramas de clases, secuencia, componentes, despliegue y actividad |

---

## 1. Alcance

Las **máquinas de estado** viven en `05. Casos de uso y diagramas de estado` §4 y el **modelo entidad-relación** en `04. Modelo de datos` §2. Aquí están los cinco que faltan: **clases**, **secuencia**, **componentes**, **despliegue** y **actividad**.

Todos en Mermaid: se versionan en Git y se editan como texto. Un diagrama que vive en un PNG deja de reflejar el sistema a la tercera semana.

---

## 2. Diagrama de clases — dominio

Modelo de dominio de `packages/shared`, compartido por cliente y servidor.

```mermaid
classDiagram
    class Comanda {
        +UUID id
        +UUID sucursalId
        +UUID corteCajaId
        +UUID~opt~ mesaId
        +TipoServicio tipoServicio
        +UUID meseroId
        +int~opt~ folio
        +EstadoComanda estado
        +Decimal total
        +DateTime abiertaAt
        +agregarLinea(producto, cantidad, notas)
        +enviarACocina()
        +marcarEntregada()
        +cobrar(pagos)
        +cancelar(motivo)
        +calcularTotal() Decimal
        +calcularMermaSiSeCancela() ResumenMerma
    }

    class ComandaDetalle {
        +UUID id
        +UUID comandaId
        +UUID productoId
        +string nombreProducto
        +Decimal precioUnitario
        +int cantidad
        +string~opt~ notas
        +EstadoLinea estado
        +DateTime~opt~ enviadaAt
        +importe() Decimal
        +generaMermaSiSeCancela() bool
    }

    class ComandaEvento {
        +UUID id
        +bigint~opt~ seq
        +UUID comandaId
        +TipoEvento tipo
        +json payload
        +UUID actorId
        +UUID dispositivoId
        +HLC hlc
        +DateTime tsCliente
    }

    class ComandaDomicilio {
        +UUID comandaId
        +string nombreCliente
        +string telefono
        +string direccion
        +string~opt~ referencias
    }

    class Pago {
        +UUID id
        +MetodoPago metodo
        +Decimal monto
        +Decimal~opt~ recibido
        +Decimal~opt~ cambio
        +calcularCambio() Decimal
    }

    class MermaProducto {
        +UUID id
        +UUID detalleId
        +string nombreProducto
        +int cantidad
        +Decimal costoEstimado
        +EstadoLinea estadoAlCancelar
        +string motivo
    }

    class CorteCaja {
        +UUID id
        +EstadoCorte estado
        +Decimal fondoInicial
        +Decimal~opt~ contadoEfectivo
        +abrir(fondo)
        +cerrar(contado, motivo)
        +esperadoEfectivo() Decimal
        +diferencia() Decimal
        +puedeCerrarse() bool
    }

    class Producto {
        +UUID id
        +string nombre
        +Decimal precioBase
        +bool disponible
        +bool activo
        +precioPara(sucursalId) Decimal
    }

    Comanda "1" *-- "1..*" ComandaDetalle
    Comanda "1" o-- "0..*" ComandaEvento
    Comanda "1" o-- "0..*" Pago
    Comanda "1" o-- "0..1" ComandaDomicilio
    Comanda "1" o-- "0..*" MermaProducto
    ComandaDetalle "1" o-- "0..1" MermaProducto
    CorteCaja "1" o-- "0..*" Comanda
    Producto "1" ..> "0..*" ComandaDetalle : snapshot
```

**La relación `Producto ..> ComandaDetalle` es una dependencia punteada, no una asociación sólida, y ahí está lo importante.** La línea **copia** nombre y precio en el momento de la venta (ADR-006); no los lee del producto vivo. Dibujar una asociación sólida sugeriría que la línea consulta al producto para calcular su importe — que es precisamente el bug que ADR-006 previene.

**`Comanda` es la raíz de agregado.** `ComandaDetalle` no existe fuera de ella (composición, rombo lleno). `Pago`, `ComandaEvento` y `MermaProducto` son agregación: sobreviven conceptualmente como registros contables aunque la comanda se cancele.

**`calcularMermaSiSeCancela()` existe en el modelo porque RF-E-18 lo exige.** La advertencia al mesero necesita el cálculo *antes* de cancelar. No es un efecto secundario de la cancelación: es una consulta previa.

---

## 3. Diagrama de clases — servicios

```mermaid
classDiagram
    class ProyectorEstado {
        <<pure>>
        +proyectarEstadoComanda(comanda, lineas, eventos) EstadoComanda
        +proyectarEstadoLinea(linea, eventos) EstadoLinea
        +plegarEventos(eventos) EstadoComanda
    }

    class MaquinaEstados {
        <<pure>>
        +transicionValida(desde, hasta, actor) bool
        +resolverConflicto(eventoA, eventoB) Resolucion
        +eventosPermitidosPara(rol) TipoEvento[]
    }

    class RelojHLC {
        -DateTime ultimoFisico
        -int contador
        -string idNodo
        +ahora() HLC
        +recibir(hlcRemoto) HLC
        +comparar(a, b) int
    }

    class ServicioSync {
        -ColaOutbox outbox
        -bigint cursor
        +encolar(evento)
        +push() Promise
        +pull() Promise
        +alReconectar()
    }

    class ColaOutbox {
        <<Dexie>>
        +agregar(evento)
        +pendientes() Evento[]
        +marcarEnviando(ids)
        +marcarConfirmado(ids)
        +marcarConflicto(id, razon)
        +conteoPendientes() int
    }

    class RepositorioComanda {
        <<Drizzle>>
        +insertarEventos(eventos) Resultado
        +asignarFolio(comandaId) int
        +proyectar(comandaId)
        +abiertasPor(sucursalId) Comanda[]
    }

    class ServicioNotificacion {
        +alSuscribir(sucursalId, ws)
        +emitir(sucursalId, evento)
        -escucharNotify()
    }

    ServicioSync --> ColaOutbox
    ServicioSync --> RelojHLC
    ServicioSync ..> ProyectorEstado
    RepositorioComanda ..> ProyectorEstado
    RepositorioComanda ..> ServicioNotificacion
    ProyectorEstado ..> MaquinaEstados
```

**`ProyectorEstado` y `MaquinaEstados` están marcados `<<pure>>` y eso es una restricción de diseño, no una etiqueta.** Sin E/S, sin fecha del sistema, sin aleatoriedad: entradas → salida. Tres consecuencias:

1. **Corren idénticos en cliente y servidor** (RNF-M-7, RNF-I-6). Es la única forma de garantizar que no divergen.
2. **Se prueban con property-based testing.** Se generan secuencias aleatorias de eventos y se verifica convergencia. Sin pureza, no se puede.
3. **Cualquier estado se reconstruye plegando el log**, en cualquier momento, sin efectos secundarios.

**`RelojHLC.recibir()` no es un `setter`.** Al recibir un HLC remoto, el reloj local avanza si el remoto va adelante — así se propaga el orden causal entre dispositivos. Es lo que hace que el HLC funcione con relojes desfasados (ADR-003).

---

## 4. Diagrama de secuencia — comanda con red

Flujo feliz: CU-01 → CU-02 → CU-03.

```mermaid
sequenceDiagram
    actor M as Mesero
    participant T as Tablet (PWA)
    participant D as Dexie (outbox)
    participant A as API Fastify
    participant P as PostgreSQL
    participant W as WebSocket
    participant C as Pantalla Cocina

    M->>T: Agrega 3 tacos de pastor
    T->>T: id = UUIDv7()
    T->>T: snapshot precio y nombre
    T->>T: hlc = reloj.ahora()
    T->>D: encolar(linea_agregada)
    T-->>M: UI actualizada (< 100 ms)

    M->>T: Enviar a cocina
    T->>D: encolar(comanda_enviada)
    T-->>M: "Enviando..."

    D->>A: POST /sync/push [eventos]
    A->>A: valida sesión, rol y sucursal
    A->>P: BEGIN
    A->>P: INSERT eventos ON CONFLICT DO NOTHING
    A->>P: asignar folio (#42)
    A->>P: proyectar comanda.estado
    A->>P: NOTIFY canal_sucursal
    A->>P: COMMIT
    A-->>D: 200 {confirmados, folio: 42}
    D->>T: marcar confirmada
    T-->>M: "✓ En cocina · #42"

    P->>W: NOTIFY
    W->>C: evento por WebSocket
    C-->>C: Suena alerta, aparece #42
```

**El `NOTIFY` va dentro de la transacción y se entrega al hacer `COMMIT`.** Postgres lo garantiza. Emitirlo fuera abriría una ventana donde la cocina ve una comanda que aún podría revertirse.

**El folio se asigna en el servidor** (ADR-003) y vuelve en la respuesta. La tablet lo adopta. Antes de ese momento el mesero ve un identificador local.

**La UI responde en el cuarto paso, mucho antes de que la red intervenga.** Esa es la razón de RNF-P-1 y de que el mesero no espere jamás.

---

## 5. Diagrama de secuencia — sin red y reconexión

El caso que define la arquitectura (CU-14, ADR-004 Nivel 1).

```mermaid
sequenceDiagram
    actor M as Mesero
    participant T as Tablet
    participant D as Dexie (outbox)
    participant A as API
    participant C as Cocina

    Note over T,A: ❌ Se cae el internet

    M->>T: Crea comanda, 3 tacos
    T->>D: encolar(comanda_creada, linea_agregada)
    T-->>M: UI actualizada normal
    M->>T: Enviar a cocina
    T->>D: encolar(comanda_enviada)
    T-->>M: ⚠️ "Solo en este dispositivo (3)"

    Note over M,C: El mesero VE que cocina no la tiene (RF-E-14).<br/>Se lo dice de viva voz, como hoy.

    M->>T: Levanta 2 comandas más
    T->>D: encolar(...)
    T-->>M: ⚠️ "Sin conexión · 9 pendientes"

    Note over T,A: ✅ Vuelve el internet

    T->>T: detecta red
    T->>D: pendientes()
    D->>A: POST /sync/push [9 eventos]
    A->>A: valida cada evento
    A-->>D: 200 {confirmados: 9, folios: [42,43,44]}
    D->>T: marcar confirmados
    T-->>M: ✓ "Sincronizado"

    T->>A: GET /sync/pull?desde=1180
    A-->>T: eventos ajenos [seq 1181..1195]
    T->>T: plegar por HLC + proyectar
    T->>T: guardar cursor = 1195

    A->>C: WebSocket: 3 comandas nuevas
    C-->>C: Aparecen #42, #43, #44
```

**El aviso "Solo en este dispositivo" es el requisito más honesto del sistema** (RF-E-14). No esconde la limitación del Nivel 1: la hace visible para que el mesero decida. Un indicador que mintiera aquí sería peor que no tenerlo.

**Push antes que pull, siempre.** Empujar primero asegura que el trabajo local no se pierda si el pull falla. Al revés, un pull que trae eventos conflictivos podría alterar el estado local antes de que sus eventos existan en el servidor.

**Nada se pierde. Solo llega tarde.** Ese es el trato completo del Nivel 1.

---

## 6. Diagrama de secuencia — cancelación con merma

CU-06, el flujo con más reglas de negocio del sistema.

```mermaid
sequenceDiagram
    actor M as Mesero
    participant T as Tablet
    participant S as ProyectorEstado
    participant A as API
    participant P as PostgreSQL
    participant C as Cocina

    M->>T: Cancelar comanda #42
    T->>S: calcularMermaSiSeCancela(#42)
    S->>S: filtrar líneas en_preparacion o lista
    S-->>T: {2 tacos pastor, $36}

    T-->>M: ⚠️ "2 tacos de pastor ya preparados.<br/>Cancelar registra $36 de merma. ¿Continuar?"

    Note over M: El precio hace consciente la decisión (RF-E-18)

    M->>T: Confirma + motivo: "cliente se fue"
    T->>T: hlc = reloj.ahora()
    T->>T: encolar(comanda_cancelada)
    T->>T: encolar(merma_producto) × líneas preparadas
    T-->>M: Comanda cancelada

    T->>A: POST /sync/push
    A->>A: valida rol: ¿puede cancelar? ✓
    A->>P: BEGIN
    A->>P: INSERT comanda_evento
    A->>P: INSERT merma_producto (CHECK ya se preparó)
    A->>P: UPDATE comanda.estado = cancelada
    A->>P: NOTIFY
    A->>P: COMMIT
    A-->>T: 200

    P->>C: WebSocket
    C-->>C: 🔴 "CANCELADA #42 — dejen de prepararla"

    Note over P: El Admin la ve en su apartado<br/>con motivo, autor y costo (RF-I-9)
```

**La consulta de merma ocurre *antes* de cancelar**, sobre la función pura del proyector. La advertencia necesita el número exacto para que la decisión sea informada.

**Las líneas en `pendiente` no generan `merma_producto`** (RF-E-20): cocina ni las había empezado. El `CHECK solo_si_ya_se_preparo` lo hace cumplir en la base, no solo en el cliente.

**Y si cocina marcó `lista` justo en ese instante:** la cancelación gana (RF-E-21) y la merma se registra con el estado real según el orden HLC. Ver la matriz de conflictos en `05` §5.

---

## 7. Diagrama de componentes

```mermaid
graph TB
    subgraph Cliente["🖥️ apps/web — PWA"]
        UI[Componentes React<br/>shadcn/ui]
        SW[Service Worker<br/>caché de app]
        Q[TanStack Query]
        DX[(Dexie<br/>IndexedDB)]
        SY[ServicioSync]
        WS1[Cliente WebSocket]
    end

    subgraph Shared["📦 packages/shared"]
        PROJ[ProyectorEstado ⟨pure⟩]
        FSM[MaquinaEstados ⟨pure⟩]
        HLC[RelojHLC]
        TYPES[Tipos + esquemas Zod]
    end

    subgraph API["⚙️ apps/api — Fastify"]
        AUTH[Autenticación<br/>JWT + PIN]
        AUTHZ[Autorización<br/>rol + sucursal]
        SYNC[Endpoints de sync<br/>push/pull]
        REST[REST: catálogo,<br/>reportes, admin]
        WSS[Servidor WebSocket]
        NOTIF[ServicioNotificacion<br/>LISTEN]
    end

    subgraph DB["🗄️ packages/db"]
        DRZ[Esquema Drizzle]
        MIG[Migraciones]
    end

    PG[(PostgreSQL 16)]

    UI --> Q
    UI --> SY
    Q --> SY
    SY --> DX
    SY --> HLC
    SY --> PROJ
    SY -.HTTPS.-> SYNC
    WS1 -.WSS.-> WSS
    SW --> UI

    PROJ --> FSM
    UI --> TYPES
    SYNC --> TYPES

    SYNC --> AUTHZ
    REST --> AUTHZ
    AUTHZ --> AUTH
    SYNC --> PROJ
    SYNC --> DRZ
    REST --> DRZ
    SYNC --> NOTIF
    NOTIF --> WSS
    DRZ --> PG
    NOTIF -.LISTEN/NOTIFY.-> PG

    style Shared fill:#2d4a3e,color:#fff
    style PROJ fill:#1e5f3f,color:#fff
    style FSM fill:#1e5f3f,color:#fff
```

**`packages/shared` está resaltado porque es el corazón de la corrección del sistema.** `ProyectorEstado` y `MaquinaEstados` los importan **cliente y servidor**, tal cual, sin adaptadores. Si esa lógica se implementara dos veces, algún día divergirían — y el bug aparecería en producción, en hora pico, sin forma de reproducirlo (RNF-M-7, RNF-I-6).

**`AUTHZ` está entre todo endpoint y los datos**, incluido el de sync. Un evento empujado se autoriza igual que una petición REST, porque eso es (RS-Y-1).

**`NOTIF` escucha Postgres con `LISTEN` y reenvía por WebSocket.** No hay broker. A 4 dispositivos por sucursal, meter Redis sería infraestructura que mantener sin problema que resolver (ADR §6).

---

## 8. Diagrama de despliegue — MVP (Nivel 1)

```mermaid
graph TB
    subgraph Sucursal["🏪 Sucursal"]
        T1["📱 Tablet Mesero 1<br/>Android/iPad<br/>PWA + IndexedDB"]
        T2["📱 Tablet Mesero 2"]
        KDS["🖥️ Pantalla Cocina<br/>Tablet 10&quot; o monitor"]
        R["📶 Router WiFi"]
        T1 --- R
        T2 --- R
        KDS --- R
    end

    subgraph Admin["🏠 Administrador"]
        PC["💻 Navegador<br/>escritorio"]
    end

    I(("☁️ Internet"))
    R --- I
    PC --- I

    subgraph VPS["🖧 VPS — Docker Compose"]
        CAD["Caddy<br/>:443 · TLS auto"]
        subgraph Net["red interna docker"]
            API["lena-api<br/>Node 22 + Fastify<br/>usuario no-root"]
            PG[("lena-db<br/>PostgreSQL 16<br/>sin puerto público")]
        end
        BK["Respaldo diario<br/>cifrado → externo"]
        CAD --> API
        API --> PG
        PG -.-> BK
    end

    I -.HTTPS/WSS.-> CAD

    style VPS fill:#1a2332,color:#fff
    style Net fill:#243447,color:#fff
```

| Nodo | Software | Notas |
|---|---|---|
| Tablet Mesero | PWA en Chrome/Safari | ≥ 8", 1280×800 (RNF-C-2). Datos: solo turno abierto (RS-L-1) |
| Pantalla Cocina | PWA, siempre encendida | Texto ≥ 18 px (RNF-U-5) |
| VPS | Docker Compose | Solo 443 y SSH expuestos (RS-T-5) |
| PostgreSQL | 16, red interna | Sin puerto público (RS-T-6) |
| Respaldos | Diarios, cifrados, fuera del VPS | Retención 30 días (RS-T-14) |

**Si muere el internet, las tablets siguen operando** con su copia local, pero **no se ven entre sí**: no hay nada en la sucursal que las conecte. Ese es el límite exacto del Nivel 1 (ADR-004) y es lo que el siguiente diagrama resuelve.

---

## 9. Diagrama de despliegue — Fase 3 (Nivel 2)

```mermaid
graph TB
    subgraph Sucursal["🏪 Sucursal"]
        T1["📱 Tablet Mesero 1"]
        T2["📱 Tablet Mesero 2"]
        KDS["🖥️ Pantalla Cocina"]
        R["📶 Router WiFi"]

        subgraph Edge["📦 Nodo de sucursal — Mini PC"]
            EAPI["lena-api<br/>⭐ MISMA IMAGEN"]
            EPG[("PostgreSQL 16<br/>local")]
            EAPI --> EPG
        end

        T1 --- R
        T2 --- R
        KDS --- R
        R --- EAPI
        R --- I(("☁️ Internet"))
    end

    subgraph VPS["🖧 VPS — Nube"]
        CAPI["lena-api"]
        CPG[("PostgreSQL 16<br/>consolidado")]
        CAPI --> CPG
    end

    PC["💻 Admin"] --- I
    I -.-> CAPI
    EPG <-.->|"sync bidireccional<br/>MISMO PROTOCOLO"| CPG

    style Edge fill:#1e5f3f,color:#fff
    style EAPI fill:#2d7a52,color:#fff
```

**Todo el argumento de ADR-001 está en este diagrama.** El nodo de sucursal corre **la misma imagen Docker** que el VPS y habla **el mismo protocolo de sync** (ADR-005). Las tablets solo cambian de URL base.

**Con Supabase esto habría exigido self-hostear la plataforma entera** — una operación de otro orden de magnitud. Ese fue el costo real que se evitó pagando ~3-4 semanas extra en el MVP.

**Qué cambia entre el Nivel 1 y el Nivel 2:**

| | Cambia |
|---|---|
| Modelo de datos | ❌ Nada |
| Protocolo de sync | ❌ Nada |
| Código del cliente | ⚠️ Solo la URL base |
| Código del API | ❌ Nada |
| Despliegue | ✅ Un contenedor más, en la sucursal |
| Hardware | ✅ Mini PC (~$3,000-4,500 MXN) |

**Es un cambio de despliegue, no de arquitectura.** Que sea así hoy es lo que permite empezar barato mañana sin hipotecar el futuro.

---

## 10. Diagrama de actividad — ciclo de vida de una comanda

Recorrido completo con los tres actores, de la mesa al corte.

```mermaid
flowchart TD
    Start([Cliente llega]) --> Turno{¿Turno de<br/>caja abierto?}
    Turno -->|No| Abrir[Abrir turno<br/>con fondo inicial]
    Abrir --> Tipo
    Turno -->|Sí| Tipo{Tipo de<br/>servicio}

    Tipo -->|Mesa| Mesa[Seleccionar mesa]
    Tipo -->|Para llevar| Llevar[Generar ID local<br/>para vocear]
    Tipo -->|Domicilio| Dom[Capturar nombre,<br/>teléfono, dirección]

    Mesa --> Cap
    Llevar --> Cap
    Dom --> Cap

    Cap[Capturar líneas<br/>snapshot de precio] --> Mas{¿Más<br/>productos?}
    Mas -->|Sí| Cap
    Mas -->|No| Env[Enviar a cocina]

    Env --> Red{¿Hay red?}
    Red -->|No| Cola[Encolar en outbox<br/>⚠️ mostrar 'solo local']
    Cola --> Voz[Mesero avisa<br/>de viva voz]
    Voz --> Prep
    Red -->|Sí| Push[Push + folio + NOTIFY]
    Push --> Prep[👨‍🍳 Cocina prepara]

    Prep --> Cancel{¿Se<br/>cancela?}

    Cancel -->|Sí| Calc[Calcular merma<br/>de líneas preparadas]
    Calc --> Warn[⚠️ Advertir al mesero<br/>con costo]
    Warn --> Conf{¿Confirma?}
    Conf -->|No| Prep
    Conf -->|Sí| Motivo[Capturar motivo]
    Motivo --> Merma[Registrar merma_producto<br/>+ alertar cocina]
    Merma --> Canc([Cancelada])

    Cancel -->|No| Lista[Cocina marca lista<br/>→ notifica al mesero]
    Lista --> Entrega[🍽️ Mesero entrega<br/>→ marca entregada]
    Entrega --> Extra{¿Cliente<br/>pide más?}
    Extra -->|Sí| Cap
    Extra -->|No| Cobrar[Cobrar<br/>habilitado]

    Cobrar --> Pago[Registrar pagos<br/>efectivo/tarjeta/transfer]
    Pago --> Cuadra{¿Pagos =<br/>total?}
    Cuadra -->|No| Pago
    Cuadra -->|Sí| Cobrada([Cobrada])

    Cobrada --> Cierre{¿Fin del<br/>turno?}
    Canc --> Cierre
    Cierre -->|No| Start
    Cierre -->|Sí| Abiertas{¿Comandas<br/>abiertas?}
    Abiertas -->|Sí| Resolver[Listarlas:<br/>cobrar o cancelar]
    Resolver --> Abiertas
    Abiertas -->|No| Corte[Contar efectivo<br/>→ calcular diferencia]
    Corte --> Fin([Turno cerrado<br/>inmutable])

    style Cola fill:#7a5c1e,color:#fff
    style Warn fill:#7a3d1e,color:#fff
    style Merma fill:#7a1e1e,color:#fff
    style Canc fill:#7a1e1e,color:#fff
    style Cobrada fill:#1e5f3f,color:#fff
    style Fin fill:#1e5f3f,color:#fff
```

**Tres detalles del diagrama que son decisiones de negocio, no de dibujo:**

1. **"¿Turno abierto?" está al principio y no se puede rodear.** Lo impone el esquema con `comanda.corte_caja_id NOT NULL` (RF-H-3). Una comanda sin turno hace que el corte deje de significar algo.

2. **"¿Cliente pide más?" regresa a capturar** después de entregada. Es el caso más común de una taquería y funciona sin lógica especial gracias a la derivación del estado (`05` §4.3).

3. **La rama de cancelación pasa obligatoriamente por advertir con costo antes de confirmar** (RF-E-18). Sin ese paso, cancelar es psicológicamente gratis y la merma se vuelve invisible — que es justo el problema de la Visión §1.

---

## 11. Índice de diagramas

| Diagrama | Dónde |
|---|---|
| Entidad-relación | `04. Modelo de datos` §2 |
| Estados — línea de comanda | `05. Casos de uso` §4.1 |
| Estados — comanda | `05. Casos de uso` §4.2 |
| Estados — corte de caja | `05. Casos de uso` §4.4 |
| Estados — outbox de sync | `05. Casos de uso` §4.5 |
| Casos de uso | `05. Casos de uso` §2 |
| Clases — dominio | Este documento §2 |
| Clases — servicios | Este documento §3 |
| Secuencia — comanda con red | Este documento §4 |
| Secuencia — offline y reconexión | Este documento §5 |
| Secuencia — cancelación con merma | Este documento §6 |
| Componentes | Este documento §7 |
| Despliegue — MVP | Este documento §8 |
| Despliegue — Fase 3 | Este documento §9 |
| Actividad — ciclo de vida | Este documento §10 |
